#!/bin/sh
# switch to dwm session, need to login again

killall bar
cd /home/niko/.local/src/dwm; sudo make clean install
rm ~/.xprofile
ln -s ~/.config/x11/dwmprofile ~/.xprofile
killall -KILL -u niko
